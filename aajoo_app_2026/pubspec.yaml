name: rent_home
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+9

environment:
  sdk: ^3.5.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  google_fonts: ^8.1.0
  google_maps_flutter: ^2.9.0
  geolocator: ^13.0.1
  fbroadcast: ^2.0.0
  carousel_slider: ^5.0.0
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  calendar_date_picker2: ^1.1.7
  image_picker: ^1.1.2
  file_picker: ^8.1.2
  http: ^1.3.0
  intl: ^0.19.0
  lottie: ^3.1.3
  curved_navigation_bar: ^1.0.6
  razorpay_flutter: ^1.3.7
  fluttertoast: ^8.2.8
  ionicons:
    path: packages/ionicons
  flutter_local_notifications: ^18.0.1
  permission_handler: ^11.4.0
  iconsax: ^0.0.8
  firebase_messaging: ^15.2.4
  firebase_core: ^3.12.1
  # Google Sign-In. firebase_auth turns the Google credential into a Firebase
  # ID token, which is what the backend verifies — the raw google_sign_in token
  # is issued by Google, not Firebase, and verifyIdToken would reject it.
  firebase_auth: ^5.5.1
  google_sign_in: ^6.3.0
  get: ^4.6.6
  flutter_secure_storage: ^9.2.2
  dio: ^5.7.0
  pin_code_fields: ^8.0.1
  animations:
  flutter_staggered_animations:
  app_settings: ^5.1.1
  geocoding: ^3.0.0
  url_launcher: ^6.3.1
  provider: ^6.1.4
  socket_io_client: ^3.1.2
  timeago: ^3.7.1
  animate_do: ^4.2.0
  pdf: ^3.10.8
  printing: ^5.12.0
  path_provider: ^2.1.1
  share_plus:
  # carousel_slider: ^5.0.0
  flutter_rating_bar: 
  flutter_country_state: ^1.0.1 
  csc_picker: ^0.2.7
  flutter_holo_date_picker: ^2.0.0
  pretty_dio_logger: ^1.4.0
  logger: ^2.6.0
  webview_flutter: ^4.7.0
  



dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0
  change_app_package_name: ^1.5.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/images/
    - assets/aajoo_new_logo.png
    - google_fonts/
    - lib/utils/csc_picker/assets/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    # Bundled so the app never fetches its own typeface over the network — see
    # lib/utils/fonts.dart. Variable fonts: weight is chosen via fontVariations.
    - family: PlusJakartaSans
      fonts:
        - asset: assets/google_fonts/PlusJakartaSans-VF.ttf
    - family: Manrope
      fonts:
        - asset: assets/google_fonts/Manrope-VF.ttf
    # Poppins is the display face of the client's reference app. Static
    # weights, not a variable file — Google Fonts does not publish a Poppins
    # VF — so each weight is registered on its own and chosen with
    # fontWeight rather than a wght axis.
    - family: Poppins
      fonts:
        - asset: assets/google_fonts/Poppins-Regular.ttf
          weight: 400
        - asset: assets/google_fonts/Poppins-Medium.ttf
          weight: 500
        - asset: assets/google_fonts/Poppins-SemiBold.ttf
          weight: 600
        - asset: assets/google_fonts/Poppins-Bold.ttf
          weight: 700
    - family: Mon
      fonts:
        - asset: assets/Montserrat-Regular.ttf
    - family: Montserrat
      fonts:
        - asset: google_fonts/Montserrat-Regular.ttf
          weight: 400

    - family: Lato
      fonts:
        - asset: google_fonts/Lato-Regular.ttf
          weight: 400
        - asset: google_fonts/Lato-Bold.ttf
          weight: 700
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
